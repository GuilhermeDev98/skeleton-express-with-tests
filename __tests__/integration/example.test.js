describe('Example', () => {
    it('check if we are in the matrix', () => {
        const dejavu = true
        console.log("I don't know, but I'm having a dejavú")
        expect(dejavu).toBe(true)
    })
})